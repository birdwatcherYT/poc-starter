# `cp terraform.tfvars.example terraform.tfvars` してから値を埋めて使う。
# 実値の terraform.tfvars は .gitignore 済み（プロジェクト ID やグループ名は環境固有のためコミットしない）。
#
# project_id は `make set-project PROJECT_ID=<id>` で自動書き込みされるので
# このファイルで設定する必要は通常ない（直接書いてもよい）。
# project_id = "your-gcp-project-id"

# --- 任意: アクセス制御 ---

# Cloud SQL に IAM 認証で接続する開発者の Google Group。
# 空文字なら IAM グループユーザーを作らない（アプリ SA のパスワード認証のみ）。
developer_group = "devs@example.com"

# API のアクセスモード:
#   - "browser"  : Cloud Run IAP を有効化。ブラウザから OAuth でアクセスできる。
#                  ターミナル/curl から叩くには手動で OAuth client を programmatic_clients に登録する必要あり。
#   - "terminal" : IAP を無効化し、Google Group に直接 roles/run.invoker を付与。
#                  `gcloud auth print-identity-token` の ID token で curl 可。ブラウザからは認証画面が出ない。
access_mode = "browser"

# アクセスを許可する Google Group（access_mode に応じて IAP 経由 or Cloud Run invoker として付与）。
# 空文字ならバインドをスキップ（外部からアクセス不可になるので注意）。
allowed_group = "api-users@example.com"

# --- 任意: リソース名・リージョンの上書き（不要なら省略可） ---
# region        = "asia-northeast1"
# service_name  = "poc-starter-api"
# instance_name = "poc-starter-db"
# db_name       = "poc"
# image_tag     = "latest"
