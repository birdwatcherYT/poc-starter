"""poc-starter-database: SQLAlchemy スキーマ定義 + Alembic マイグレーション。

api ランタイムからは `from schema.models import Message` のように使う。
全モデルの基底クラスは `from schema.base import Base`。

ワークフローや責務分担の詳細は `database/README.md` を参照。
"""
